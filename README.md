This repository contains the frontend for a full-stack video/social platform.
It is built using React (Vite) and communicates with an existing backend via REST APIs.

The backend is maintained in a separate repository. This frontend focuses on UI, client-side logic and API integration.